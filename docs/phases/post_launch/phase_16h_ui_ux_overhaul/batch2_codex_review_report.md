# Phase 16H Batch 2 Codex Review Report

## Status

PASS WITH REPAIRS

Batch 2 is approved after focused Codex repairs. Review and repairs stayed inside jobs list/detail scope. No Batch 3 work was started.

## Files Changed By Gemini

- `apps/jobs/tests/test_14j_job_card_skills.py`
- `apps/jobs/tests/test_15e_eligibility.py`
- `apps/jobs/tests/test_views.py`
- `apps/jobs/views.py`
- `apps/matching/tests.py`
- `static/css/app.css`
- `templates/jobs/job_detail.html`
- `templates/jobs/job_list.html`
- `templates/jobs/partials/job_card.html`
- `templates/jobs/partials/job_filter_panel.html`
- `templates/jobs/partials/save_button.html`
- `apps/jobs/services/cta_context.py`
- `docs/phases/post_launch/phase_16h_ui_ux_overhaul/batch2_gemini_report.md`

## Files Changed By Codex

- `apps/jobs/services/cta_context.py`
  - Corrected readiness to require an existing profile plus profile completion >= 50 or an active parsed CV.
  - Removed the invalid interpretation of `llm_explanation_status="failed"` as a failed deterministic match.
- `apps/jobs/views.py`
  - Populated current-page `job.is_saved` for authenticated job-list rendering.
- `templates/jobs/job_list.html`
  - Hid the saved-jobs stat from anonymous users.
  - Added scoped `.jobs-v16` wrapper.
- `templates/jobs/job_detail.html`
  - Added scoped `.jobs-v16` wrapper.
  - Kept anonymous sign-in CTA on existing allauth login route with `next`.
- `templates/jobs/partials/save_button.html`
  - Added internal authenticated guard.
  - Restored HTMX behavior by always emitting `hx-post`, `hx-target`, and `hx-swap` while keeping normal POST fallback.
- `static/src/css/app.css`
  - Added scoped Batch 2 jobs prototype classes under `.jobs-v16`.
- `static/css/app.css`
  - Regenerated by `npm run css:build`.
- `apps/jobs/tests/test_views.py`
  - Added regression coverage for anonymous Save hiding, logged-in Save/Unsave, HTMX save partial behavior, job-detail CTA states, match `public_id`, and LLM explanation failure handling.
  - Removed weakened placeholder test body by replacing it with relevant behavior coverage elsewhere in the file.
- `apps/jobs/tests/test_14j_job_card_skills.py`
- `apps/jobs/tests/test_15e_eligibility.py`
- `templates/jobs/job_detail.html`
- `templates/jobs/job_list.html`
  - Removed trailing whitespace.
- `docs/phases/post_launch/phase_16h_ui_ux_overhaul/batch2_codex_review_report.md`

## Scope Violations

None after repair.

No forbidden Batch 3+ areas were changed: `templates/base.html`, account/auth templates, dashboard/profile/settings templates, recommendations templates, saved jobs page template, matching score templates, About/contact backend, notification feed/bell/model/page, models, migrations, matching score formulas, recommendation algorithms, LLM/OpenRouter paths, and France Travail ingestion/search paths were not changed.

`apps/matching/tests.py` was touched by Gemini only to remove an obsolete job-detail anonymous quick-match UI assertion. Backend quick-match behavior remains covered and unchanged.

## Behavior Verdicts

Anonymous Save:

- PASS. Anonymous job cards do not render Save/Unsave forms or `jobs:save`/`jobs:unsave` actions.
- PASS. Anonymous job detail does not render Save.
- PASS. Anonymous saved-jobs stat/box was removed from the jobs list hero by Codex.

Logged-in Save:

- PASS. Logged-in job cards render Save/Unsave.
- PASS. Current-page saved state is populated in `apps/jobs/views.py`.
- PASS. Save partial remains normal POST-capable and HTMX-capable through `hx-post`, `hx-target`, and `hx-swap`.

Job detail CTA matrix:

- PASS. Anonymous users see a sign-in CTA using `account_login` with `next` to the current job detail path.
- PASS. Anonymous users do not see anonymous quick-match UI on job detail.
- PASS. Logged-in users without profile/CV readiness see `Compléter profil / CV`.
- PASS. Logged-in ready users without a match see `Calculer la compatibilité`.
- PASS. Logged-in users with an existing match see `Voir le score`.
- PASS. Existing match score links use `match.public_id`, not integer ID.
- PASS. Failed/stale/refresh match state was not invented. `llm_explanation_status="failed"` is not treated as match failure because deterministic scoring still exists.

France locked UI:

- PASS. Filter panel shows France as checked/disabled.
- PASS. No fake countries were added.
- PASS. Jobs list copy remains France-only; no future-country overpromise was added.

## Backend Safety Verdict

PASS.

- Job search remains local PostgreSQL through `JobSearchService` and `JobEligibilityService`.
- No external API calls were added to jobs list/detail.
- No France Travail live search path was added to user search.
- No OpenRouter or LLM path was touched.
- No deterministic matching score formula was changed.
- No recommendation algorithm was changed.
- CV privacy is respected: user-facing Batch 2 logic uses `CVUpload.objects`, not `CVUpload.all_objects`.
- Public job list/detail remain anonymous-accessible.
- Public URLs continue to use UUID `public_id`.

## Tests And Command Results

```text
python manage.py check --settings=config.settings.local
System check identified no issues (0 silenced).
```

```text
python manage.py makemigrations --check --dry-run --settings=config.settings.local
No changes detected
```

```text
python manage.py test apps.jobs --settings=config.settings.local
Ran 193 tests in 51.046s
OK
Found 193 test(s).
System check identified no issues (0 silenced).
```

```text
python manage.py test apps.matching --settings=config.settings.local
Ran 39 tests in 2.832s
OK
Found 39 test(s).
System check identified no issues (0 silenced).
```

```text
python manage.py test --settings=config.settings.local
Ran 636 tests in 105.396s
OK
Found 636 test(s).
System check identified no issues (0 silenced).
```

Full suite count: 636 tests.

## CSS Build Result

PASS.

```text
npm run css:build

> tunitech-abroad@1.0.0 css:build
> tailwindcss -i ./static/src/css/app.css -o ./static/css/app.css --minify

Browserslist: caniuse-lite is outdated. Please run:
  npx update-browserslist-db@latest
  Why you should do it regularly: https://github.com/browserslist/update-db#readme

Rebuilding...

Done in 917ms.
```

The Browserslist message is non-fatal and matches prior phase output.

## Whitespace Result

Initial whitespace checks failed on trailing spaces introduced by Batch 2 edits. Codex repaired them.

Final results:

```text
git diff --check
PASS, no output
```

```text
python - <<'PY'
...
PY
PASS: no trailing whitespace or CR characters in changed/untracked text files
```

## Required Fixes Before Senior Approval

None.

## Phase Boundary Confirmation

Confirmed. Work stopped after the Batch 2 review report. No commit, push, deploy, Batch 3 auth/profile/settings work, Batch 4 recommendations/saved/match redesign, Batch 5 About/contact backend, models, migrations, notification system, LLM integration, France Travail live search, scoring formula, or recommendation algorithm work was performed.
